rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // JTI Store: 誰でも読み書き可能（エミュレータ用、本番では制限）
    match /jti_store/{document=**} {
      allow read, write: if true;
    }
  }
}
